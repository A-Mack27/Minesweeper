@model CST_350_Minesweeper_Website.Models.BoardModel

@{
	CellModel cell;
	string buttonClass = "";
	switch (Model.Size)
	{
		case 10: buttonClass = "small-game-button"; break;
		case 15: buttonClass = "medium-game-button"; break;
		case 20: buttonClass = "large-game-button"; break;
	}
}

@* Surround the entire board as a form that can be submitted *@
<form method="post">
	<div class="button-zone">
		@* Iterate over the buttons and display them *@

		@for (int i = 0; i < Model.Size; i++)
		{
			
			@for (int j = 0; j < Model.Size; j++)
			{
				<button class="@buttonClass" type="submit" name="cellLocation" value="@i,@j" asp-controller="Game" asp-action="RevealCell">
					<div class="button-label">
						@{
							cell = Model.Grid[i, j];

						}
						@if (cell.IsRevealed && (cell.LiveNeighbors > 0))
						{
							@cell.LiveNeighbors
						}
						
					</div>
				</button>
			}
			// Add a break
			<div class="line-break"></div>
		}
	</div>
</form>